# Project Context

This file contains project-specific architecture notes.

`.AGENTS/` is the project source of truth for agent behavior, including roles, constraints, schemas, and project-specific rules.
